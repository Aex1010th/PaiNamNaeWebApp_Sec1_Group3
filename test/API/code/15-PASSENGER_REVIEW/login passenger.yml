info:
  name: login passenger
  type: https
  seq: 3

https:
  method: POST
  url: "{{base_url}}auth/login"
  body:
    type: json
    raw: |
      {
        "username": "passenger123",
        "password": "passenger1234"
      }

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = JSON.parse(res.getBody());
        bru.setEnvVar("passenger_token", body.data.token);
        bru.setEnvVar("passenger_id", body.data.user.id);

    - type: tests
      code: |-
        test("Status code 200", () => {
          expect(res.getStatus()).to.equal(200);
        });
        
        test("Response time is less than 200ms", () => {
          expect(res.getResponseTime()).to.be.below(200);
        });