info:
  name: login admin
  type: https
  seq: 1

https:
  method: POST
  url: "{{base_url}}auth/login"
  body:
    type: json
    raw: |
      {
        "username": "admin123",
        "password": "123456789"
      }

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = JSON.parse(res.getBody());
        bru.setEnvVar("admin_token", body.data.token);
        bru.setEnvVar("admin_id", body.data.user.id);

    - type: tests
      code: |-
        test("Status code 200", () => {
          expect(res.getStatus()).to.equal(200);
        });
        
        test("Response time is less than 200ms", () => {
          expect(res.getResponseTime()).to.be.below(200);
        });